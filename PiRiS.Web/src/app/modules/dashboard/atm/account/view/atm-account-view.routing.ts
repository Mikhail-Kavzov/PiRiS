import { Route } from '@angular/router';
import { AtmAccountViewComponent } from './atm-account-view.component';

export const atmAccountViewRoutes: Route[] = [
    {
        path: '',
        component: AtmAccountViewComponent
    }
];
