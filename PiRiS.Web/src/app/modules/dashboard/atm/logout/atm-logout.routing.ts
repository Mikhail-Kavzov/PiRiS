import { Route } from '@angular/router';
import { AtmLogoutComponent } from './atm-logout.component';

export const atmLogoutRoutes: Route[] = [
    {
        path: '',
        component: AtmLogoutComponent,
    }
];
