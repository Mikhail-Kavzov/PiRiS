import { Route } from '@angular/router';
import { AtmWithdrawComponent } from './atm-withdraw.component';

export const atmWithdrawRoutes: Route[] = [
    {
        path: '',
        component: AtmWithdrawComponent,
    }
];
