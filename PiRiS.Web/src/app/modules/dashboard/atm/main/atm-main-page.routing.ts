import { Route } from '@angular/router';
import { AtmMainPageComponent } from './atm-main-page.component';

export const atmMainPageRoutes: Route[] = [
    {
        path: '',
        component: AtmMainPageComponent,
    }
];
