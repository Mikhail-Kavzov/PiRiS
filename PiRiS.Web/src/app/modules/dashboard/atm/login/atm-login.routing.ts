import { Route } from '@angular/router';
import { AtmLoginComponent } from './atm-login.component';

export const atmLoginRoutes: Route[] = [
    {
        path: '',
        component: AtmLoginComponent,
    }
];
