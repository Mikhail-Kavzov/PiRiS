import { Route } from '@angular/router';
import { AtmTransferComponent } from './atm-transfer.component';

export const atmTransferRoutes: Route[] = [
    {
        path: '',
        component: AtmTransferComponent,
    }
];
